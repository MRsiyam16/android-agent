uiautomator2>=3.2.0
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
websockets>=12.0
requests>=2.31.0
pydantic>=2.7.0
python-multipart>=0.0.9
anthropic>=0.40.0

# Chat agent (Agent tab). claude-agent-sdk drives the locally installed Claude Code CLI,
# which must be installed separately and signed in:
#     npm i -g @anthropic-ai/claude-code   &&   claude   (once, to authenticate)
# Signing in with a Pro/Max subscription is what keeps planner calls off metered API billing.
claude-agent-sdk>=0.2.128
python-dotenv>=1.0.0
httpx>=0.27.0

# Web adapter (testing websites). pip alone does not fetch browser binaries — run
# `playwright install chromium` once after installing this.
playwright>=1.40.0
